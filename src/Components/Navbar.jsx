import { Navbar } from "react-bootstrap";

const NavBar = () => {
    return <>
    <Navbar bg="dark" variant="dark" expand= "lg">
            <Container>
     <NavBar.brand onClick={(} => {navigate}(/)</Container>)>Pokedex</NavBar.brand>
    </Container>
    </Navbar>
    </>
}
 
export default ;